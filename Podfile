# Uncomment the next line to define a global platform for your project
# platform :ios, '18.0'

target 'Ratios' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Ratios

  pod 'Shake', :git => 'ssh://git@github.com/shakebugs/shake-ios.git'
  
  target 'RatiosTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RatiosUITests' do
    # Pods for testing
  end

end
